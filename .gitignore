.env

# Virtual environment
venv/
env/
.venv/
ENV/
env.bak/
venv.bak/
# Poetry virtual environment (usually in project root or .venv)
.python-version

# Model files and caches
*.bin
*.safetensors
*.gguf
finetuned_model/
my_finetuned_model/
.cache/

# QLoRA adapter files (keep adapter_config.json for metadata, but ignore weights)
tuned_models/**/adapter_model.safetensors
tuned_models/**/adapter_model.bin

# Training checkpoints (ignore checkpoint directories, keep final models)
tuned_models/**/checkpoint-*/
tuned_models/**/checkpoint-*/*

# Training state files (optimizer, scheduler, etc.)
tuned_models/**/optimizer.pt
tuned_models/**/scheduler.pt
tuned_models/**/scaler.pt
tuned_models/**/rng_state.pth
tuned_models/**/training_args.bin
tuned_models/**/trainer_state.json

# Model weights (keep configs and tokenizers for reproducibility)
tuned_models/**/pytorch_model.bin
tuned_models/**/model.safetensors

# Keep these for reproducibility (DO NOT ignore):
# - adapter_config.json (LoRA config)
# - tokenizer files (tokenizer.json, tokenizer_config.json, vocab.json, merges.txt)
# - model_registry.json (version tracking)
# - README.md files
# - model_info.json files
# Python cache
__pycache__/
*.py[cod]
*$py.class

# Training outputs
results/
checkpoints/
wandb/

# Experiment results (timestamped JSON files)
simrag_reproduction/experiments/**/results/*.json
simrag_reproduction/experiments/**/results/*.jsonl

# Data files
*.jsonl
*.csv

# Qdrant database (generated, should not be committed)
data/qdrant_db/

# Allow data/documents/ to be committed (source documents for reproducibility)
# data/documents/ is intentionally NOT ignored

# Python
*.pyc
*.pyo
*.pyd
.Python
*.so
*.egg
*.egg-info/
dist/
build/
*.egg-info

# Poetry
.poetry/
# Note: poetry.lock should be committed for reproducible builds

# Pytest
.pytest_cache/
.coverage
htmlcov/
*.cover
.hypothesis/
.tox/

# MyPy
.mypy_cache/
.dmypy.json
dmypy.json

# Ruff
.ruff_cache/

# Type checking
.pytype/
typecheck/

# Jupyter Notebook checkpoints (already have .ipynb_checkpoints/)
# Keeping *.ipynb ignore as is

# IDE and editors
.vscode/
.idea/
*.swp
*.swo
*~

# Jupyter Notebooks
.ipynb_checkpoints/
*.ipynb

# Logs and temporary files
*.log
*.tmp
*.temp
logs/
temp/
# Keep logs/README.md if it exists for documentation
!logs/README.md

# Environment and secrets
.env
.env.local
.env.production
.env.staging
# Allow .env.example to be tracked
!.env.example
*.key
*.pem
secrets/

# Hugging Face cache
.cache/huggingface/
transformers_cache/

# PyTorch
*.pth
*.pt

# TensorBoard
runs/
tensorboard/

# MLflow
mlruns/

# API keys and credentials
api_keys.txt
credentials.json
service_account.json

# Large datasets (uncomment if you have large data files)
# *.parquet
# *.h5
# *.hdf5
# *.pkl
# *.pickle

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Old/deprecated model directories (if any exist)
tuned_models/llama_*/
tuned_models/llama_*/*/